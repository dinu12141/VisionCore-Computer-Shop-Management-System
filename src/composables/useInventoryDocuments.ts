/**
 * DEPRECATED — This composable referenced non-existent tables (inv_documents,
 * inv_document_items, inv_document_types). All inventory document operations
 * now go through src/services/inventoryService.ts which uses the correct
 * tables: inventory_documents, inventory_document_lines.
 *
 * This file is kept empty to prevent import errors from any stale references.
 * Safe to delete once confirmed no imports remain.
 */
